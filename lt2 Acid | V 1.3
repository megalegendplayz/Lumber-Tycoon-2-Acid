local library = loadstring(game:HttpGet(('https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wall%20v3')))()

local a = library:CreateWindow("Local")
local d = library:CreateWindow("Misc")
local f = library:CreateWindow("Wood")
local h = library:CreateWindow("Plot")
local s = library:CreateWindow("Teleports")
local j = library:CreateWindow("Acid")

local b = a:CreateFolder("Humainoid") 

b:Slider("JumpPower",{
    min = 50; -- min value of the slider
    max = 500; -- max value of the slider
    precise = true; -- max 2 decimals
},function(value)
    game.Players.LocalPlayer.Character.Humanoid.JumpPower = value
end)
b:Slider("Walkspeed",{
    min = 16; -- min value of the slider
    max = 500; -- max value of the slider
    precise = true; -- max 2 decimals
},function(value)
    while true do
wait()
game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = value
end
end)
b:Button("Reset",function()
    game.Players.LocalPlayer.Character.Head:Destroy()
end)
b:Button("Infinite Jump",function()
    loadstring(game:HttpGet("https://pastebin.com/raw/V3DKCQTT"))()
end)
b:Button("Water Walker",function()
	for _,v in pairs(game.Workspace.Water:GetChildren()) do
v.CanCollide = true
end
print("Water Walker on.")
end)
b:Button("Normal Water",function()
	for _,v in pairs(game.Workspace.Water:GetChildren()) do
v.CanCollide = false
end
print("Water walker off.")
end)
b:Button("BTools",function()
	a = Instance.new("HopperBin", game.Players.LocalPlayer.Backpack)
a.BinType = 2
b = Instance.new("HopperBin", game.Players.LocalPlayer.Backpack)
b.BinType = 3
c = Instance.new("HopperBin", game.Players.LocalPlayer.Backpack)
c.BinType = 4
end)
b:Button("Hard Drag",function()
    loadstring(game:HttpGet('https://pastebin.com/raw/5b9bswMb',true))()
end)
b:Button("Ctrl + Click tp",function()
    local Player = game.Players.LocalPlayer
local Mouse = Player:GetMouse()
local UserInputService = game:GetService('UserInputService')

local HoldingControl = false

Mouse.Button1Down:connect(function()
if HoldingControl then
Player.Character:MoveTo(Mouse.Hit.p)
end
end)

UserInputService.InputBegan:connect(function(Input, Processed)
if Input.UserInputType == Enum.UserInputType.Keyboard then
if Input.KeyCode == Enum.KeyCode.LeftControl then
HoldingControl = true
elseif Input.KeyCode == Enum.KeyCode.RightControl then
HoldingControl = true 
end
end
end)

UserInputService.InputEnded:connect(function(Input, Processed)
if Input.UserInputType == Enum.UserInputType.Keyboard then
if Input.KeyCode == Enum.KeyCode.LeftControl then
HoldingControl = false
elseif Input.KeyCode == Enum.KeyCode.RightControl then
HoldingControl = false
end
end
end)
end)

local c = a:CreateFolder("Other")

c:Button("Noclip",function()
    local noclip = true char = game.Players.LocalPlayer.Character while true do if noclip == true then for _,v in pairs(char:children()) do pcall(function() if v.className == "Part" then v.CanCollide = false elseif v.ClassName == "Model" then v.Head.CanCollide = false end end) end end game:service("RunService").Stepped:wait() end
end)
c:Button("Fly (Q)",function()
    local hint = Instance.new("Hint",game.Players.LocalPlayer.PlayerGui)
    hint.Text = "Q to toggle "
    hint.Name = game.JobId
 repeat wait()
     until game.Players.LocalPlayer and game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:findFirstChild("Torso") and game.Players.LocalPlayer.Character:findFirstChild("Humanoid")
 local mouse = game.Players.LocalPlayer:GetMouse()
 repeat wait() until mouse
 local plr = game.Players.LocalPlayer
 local torso = plr.Character.Torso
 local flying = true
 local deb = true
 local ctrl = {f = 0, b = 0, l = 0, r = 0}
 local lastctrl = {f = 0, b = 0, l = 0, r = 0}
 local maxspeed = 500
 local speed = 0
  
 function Fly()
 local bg = Instance.new("BodyGyro", torso)
 bg.P = 9e4
 bg.maxTorque = Vector3.new(9e9, 9e9, 9e9)
 bg.cframe = torso.CFrame
 local bv = Instance.new("BodyVelocity", torso)
 bv.velocity = Vector3.new(0,0.1,0)
 bv.maxForce = Vector3.new(9e9, 9e9, 9e9)
 repeat wait()
 plr.Character.Humanoid.PlatformStand = false
 if ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0 then
 speed = speed+125.0+(speed/maxspeed)
 if speed > maxspeed then
 speed = maxspeed
 end
 elseif not (ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0) and speed ~= 0 then
 speed = speed-250
 if speed < 0 then
 speed = 0
 end
 end
 if (ctrl.l + ctrl.r) ~= 0 or (ctrl.f + ctrl.b) ~= 0 then
 bv.velocity = ((game.Workspace.CurrentCamera.CoordinateFrame.lookVector * (ctrl.f+ctrl.b)) + ((game.Workspace.CurrentCamera.CoordinateFrame * CFrame.new(ctrl.l+ctrl.r,(ctrl.f+ctrl.b)*.2,0).p) - game.Workspace.CurrentCamera.CoordinateFrame.p))*speed
 lastctrl = {f = ctrl.f, b = ctrl.b, l = ctrl.l, r = ctrl.r}
 elseif (ctrl.l + ctrl.r) == 0 and (ctrl.f + ctrl.b) == 0 and speed ~= 0 then
 bv.velocity = ((game.Workspace.CurrentCamera.CoordinateFrame.lookVector * (lastctrl.f+lastctrl.b)) + ((game.Workspace.CurrentCamera.CoordinateFrame * CFrame.new(lastctrl.l+lastctrl.r,(lastctrl.f+lastctrl.b)*.2,0).p) - game.Workspace.CurrentCamera.CoordinateFrame.p))*speed
 else
 bv.velocity = Vector3.new(0,0.1,0)
 end
 bg.cframe = game.Workspace.CurrentCamera.CoordinateFrame * CFrame.Angles(-math.rad((ctrl.f+ctrl.b)*50*speed/maxspeed),0,0)
 until not flying
 ctrl = {f = 0, b = 0, l = 0, r = 0}
 lastctrl = {f = 0, b = 0, l = 0, r = 0}
 speed = 0
 bg:Destroy()
 bv:Destroy()
 plr.Character.Humanoid.PlatformStand = false
 end
 mouse.KeyDown:connect(function(key)
 if key:lower() == "q" then
 if flying then flying = false
 else
 flying = true
 Fly()
 end
 elseif key:lower() == "w" then
 ctrl.f = 1
 elseif key:lower() == "s" then
 ctrl.b = -1
 elseif key:lower() == "a" then
 ctrl.l = -1
 elseif key:lower() == "d" then
 ctrl.r = 1
 end
 end)
 mouse.KeyUp:connect(function(key)
 if key:lower() == "w" then
 ctrl.f = 0
 elseif key:lower() == "s" then
 ctrl.b = 0
 elseif key:lower() == "a" then
 ctrl.l = 0
 elseif key:lower() == "d" then
 ctrl.r = 0
 end
 wait(5)
 hint:Destroy()
 end)
 Fly()
end)
c:Button("Anti-AFK",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/FBxVC1Aa"))()
end)

local z = d:CreateFolder("miscellaneous")

z:Button("Axe Dupelication",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/Kd1rvXAY"))()
end)
z:Button("Aways Day",function()
    loadstring(game:HttpGet("https://pastebin.com/raw/TusQaMzu"))()
end)
z:Button("No Fog",function()
    loadstring(game:HttpGet("https://pastebin.com/raw/wXCsYF1y"))()
end)
z:Button("Infinite Yield",function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source", true))()
end)
z:Button("Car Fly (Z)",function()
    local hint = Instance.new("Hint",game.Players.LocalPlayer.PlayerGui)
    hint.Text = "Press Z To Toggle"
    hint.Name = game.JobId
 repeat wait()
     until game.Players.LocalPlayer and game.Players.LocalPlayer.Character and game.Players.LocalPlayer.Character:findFirstChild("Torso") and game.Players.LocalPlayer.Character:findFirstChild("Humanoid")
 local mouse = game.Players.LocalPlayer:GetMouse()
 repeat wait() until mouse
 local plr = game.Players.LocalPlayer
 local torso = plr.Character.Torso
 local flying = true
 local deb = true
 local ctrl = {f = 0, b = 0, l = 0, r = 0}
 local lastctrl = {f = 0, b = 0, l = 0, r = 0}
 local maxspeed = 500
 local speed = 0
  
 function Fly()
 local bg = Instance.new("BodyGyro", torso)
 bg.P = 9e4
 bg.maxTorque = Vector3.new(9e9, 9e9, 9e9)
 bg.cframe = torso.CFrame
 local bv = Instance.new("BodyVelocity", torso)
 bv.velocity = Vector3.new(0,0.1,0)
 bv.maxForce = Vector3.new(9e9, 9e9, 9e9)
 repeat wait()
 plr.Character.Humanoid.PlatformStand = false
 if ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0 then
 speed = speed+125.0+(speed/maxspeed)
 if speed > maxspeed then
 speed = maxspeed
 end
 elseif not (ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0) and speed ~= 0 then
 speed = speed-250
 if speed < 0 then
 speed = 0
 end
 end
 if (ctrl.l + ctrl.r) ~= 0 or (ctrl.f + ctrl.b) ~= 0 then
 bv.velocity = ((game.Workspace.CurrentCamera.CoordinateFrame.lookVector * (ctrl.f+ctrl.b)) + ((game.Workspace.CurrentCamera.CoordinateFrame * CFrame.new(ctrl.l+ctrl.r,(ctrl.f+ctrl.b)*.2,0).p) - game.Workspace.CurrentCamera.CoordinateFrame.p))*speed
 lastctrl = {f = ctrl.f, b = ctrl.b, l = ctrl.l, r = ctrl.r}
 elseif (ctrl.l + ctrl.r) == 0 and (ctrl.f + ctrl.b) == 0 and speed ~= 0 then
 bv.velocity = ((game.Workspace.CurrentCamera.CoordinateFrame.lookVector * (lastctrl.f+lastctrl.b)) + ((game.Workspace.CurrentCamera.CoordinateFrame * CFrame.new(lastctrl.l+lastctrl.r,(lastctrl.f+lastctrl.b)*.2,0).p) - game.Workspace.CurrentCamera.CoordinateFrame.p))*speed
 else
 bv.velocity = Vector3.new(0,0.1,0)
 end
 bg.cframe = game.Workspace.CurrentCamera.CoordinateFrame * CFrame.Angles(-math.rad((ctrl.f+ctrl.b)*50*speed/maxspeed),0,0)
 until not flying
 ctrl = {f = 0, b = 0, l = 0, r = 0}
 lastctrl = {f = 0, b = 0, l = 0, r = 0}
 speed = 0
 bg:Destroy()
 bv:Destroy()
 plr.Character.Humanoid.PlatformStand = false
 end
 mouse.KeyDown:connect(function(key)
 if key:lower() == "z" then
 if flying then flying = false
 else
 flying = true
 Fly()
 end
 elseif key:lower() == "w" then
 ctrl.f = 1
 elseif key:lower() == "s" then
 ctrl.b = -1
 elseif key:lower() == "a" then
 ctrl.l = -1
 elseif key:lower() == "d" then
 ctrl.r = 1
 end
 end)
 mouse.KeyUp:connect(function(key)
 if key:lower() == "w" then
 ctrl.f = 0
 elseif key:lower() == "s" then
 ctrl.b = 0
 elseif key:lower() == "a" then
 ctrl.l = 0
 elseif key:lower() == "d" then
 ctrl.r = 0
 end
 wait(5)
 hint:Destroy()
 end)
 Fly()
end)

local y = d:CreateFolder("Bridges")

y:Button("Safari Bridge",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/LTpzRZ6i"))()
end)
y:Button("Gold Bridge",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/pMkcXhtL"))()
end)
y:Button("Fir Bridge",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/hViyQtqk"))()
end)
y:Button("Palm Bridge",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/1ZmG3tZ1"))()
end)

local x = f:CreateFolder("Trees")

x:Button("Bring Wood",function()
    for _, Log in pairs(game.Workspace.LogModels:GetChildren()) do
		if Log.Name:sub(1, 6) == "Loose_" and Log:findFirstChild("Owner") and Log.Owner.Value == game.Players.LocalPlayer then
			Log:MoveTo(game.Players.LocalPlayer.Character.Torso.Position + Vector3.new(0, 10, 0))
		end
	end
end)
x:Button("Bring Planks",function()
    for _, Plank in pairs(service.Workspace.PlayerModels:GetChildren()) do
		if Plank.Name=="Plank" and Plank:findFirstChild("Owner") then
			if Plank.Owner.Value == service.Players.LocalPlayer then
				Plank:MoveTo(service.Players.LocalPlayer.Character.HumanoidRootPart.Position + Vector3.new(0, 20, 0))
				for i=1,100 do
					service.ReplicatedStorage.Interaction.ClientIsDragging:FireServer(Plank)
				end
			end
		end
	end
end)
x:Button("Sell Wood (H)",function()
    Mouse = game.Players.LocalPlayer:GetMouse()	
Mouse.KeyDown:connect(function(key)
if key:lower() == "h" then	
	for _, Log in pairs(game.Workspace.LogModels:GetChildren()) do
		if Log.Name:sub(1, 6) == "Loose_" and Log:findFirstChild("Owner") then
			if Log.Owner.Value == game.Players.LocalPlayer then
				for i,v in pairs(Log:GetChildren()) do
					if v.Name=="WoodSection" then
						spawn(function()
							for i=1,10 do
								wait()
								v.CFrame=CFrame.new(Vector3.new(315, -0.296, 85.791))*CFrame.Angles(math.rad(90),0,0)
							end
						end)
					end
				end
				spawn(function()
					for i=1,20 do
						wait()
						game.ReplicatedStorage.Interaction.ClientIsDragging:FireServer(Log)
					end
				end)
			end
		end
	end
end
end)
end)
x:Button("Sell Planks (G)",function()
    Mouse = game.Players.LocalPlayer:GetMouse()
Mouse.KeyDown:Connect(function(key)	
if key:lower() == "g" then 
	for _, Plank in pairs(game.Workspace.PlayerModels:GetChildren()) do
		if Plank.Name=="Plank" and Plank:findFirstChild("Owner") then
			if Plank.Owner.Value == game.Players.LocalPlayer then
				for i,v in pairs(Plank:GetChildren()) do
					      v.Name="WoodSection" 
						spawn(function()
							for i=1,10 do
								wait()
								v.CFrame=CFrame.new(Vector3.new(315, -0.296, 85.791))*CFrame.Angles(math.rad(90),0,0)
							end
						end)
					end
				end
				spawn(function()
					for i=1,20 do
						wait()
						game.ReplicatedStorage.Interaction.ClientIsDragging:FireServer(Plank)
					end
				end)
			end
		end
	end
end)
end)

local v = f:CreateFolder("Tree Destinations")

v:Button("Cave Crawler",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(3581, -179, 430))
end)
v:Button("Palm Islands",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(2549, -5, -42))
end)
v:Button("Swamp",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-1209, 132, -801))
end)
v:Button("Lava Trees",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(-1585, 622, 1140))
end)

local u = h:CreateFolder("Main")

u:Button("Free Land",function()
    loadstring(game:HttpGetAsync("https://pastebin.com/raw/uUbT4S7Y"))()
end)
u:Button("Max Land",function()
    loadstring(game:HttpGet("https://pastebin.com/raw/gRa6JkJb"))()
end)
u:Button("Plot Teleport",function()
    for i, v in pairs(game.Workspace.Properties:GetChildren()) do
		if v.Owner.Value == game.Players.LocalPlayer then
			game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = v.OriginSquare.CFrame
		end
	end
end)

local t = s:CreateFolder("Waypoints")

t:Button("Spawn",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(155, 3, 74))
end)
t:Button("Boxed Cars",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(509, 3, -1463))
end)
t:Button("Dock",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(1114, -1, -197))
end)
t:Button("Land Store",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(258, 3, -99))
end)
t:Button("Link's Logic",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(4605, 3, -727))
end)
t:Button("Wood R US",function()
    game.Players.LocalPlayer.Character:MoveTo(Vector3.new(265, 3, 57))
end)

local k = j:CreateFolder("Credits")

k:Label("Developer: Secret",{
    TextSize = 17; 
    TextColor = Color3.fromRGB(255,255,255); 
    BgColor = Color3.fromRGB(69,69,69); 
    
}) 
k:Label("Update Coming Soon",{
    TextSize = 17; 
    TextColor = Color3.fromRGB(255,255,255); 
    BgColor = Color3.fromRGB(69,69,69); 
    
}) 
k:Label("Version: V1.3",{
    TextSize = 17; 
    TextColor = Color3.fromRGB(255,255,255); 
    BgColor = Color3.fromRGB(69,69,69); 
    
}) 
